# Budget Inteligente (IA) — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Robô semanal (Opus 4.8) gera budget sugerido + veredito + justificativa + impacto por campanha ativa; a tela mostra isso, permite aplicar/digitar budget e pausar/reativar campanha e anúncio a qualquer momento; campanha vencida vira "Encerrada".

**Architecture:** Um script Node (`coletor/budget-ia.mjs`) roda via GitHub Actions toda segunda, lê `accounts.access_token` com service role, chama o Graph direto e o Opus, e grava em `gt_budget_analises`. O front (`index.html`, monólito) lê essa tabela e injeta UI na Gestão de Tráfego, reusando o executor de escrita `_gtApplyAction`/`metaPost` e o modal `_gtConfirm` que já existem. O refino da semana é o motor client-side existente (`_gtInlineSuggest`) — nada novo no servidor.

**Tech Stack:** Node 20 (`node:test`, `fetch` nativo), Supabase (Postgres + RLS + service role), Meta Graph API v21.0, Anthropic Messages API (`claude-opus-4-8`), GitHub Actions.

## Global Constraints

- **Branch:** todo o trabalho em `feat/gt-budget-ia` (nunca commitar direto em `main`). `git config user.email` deve ficar `breno@rbvcompany.com` (email vazio TRAVA o build no Vercel).
- **Chave Anthropic:** só via secret `ANTHROPIC_API_KEY_BUDGET` no GitHub. Nunca hardcode, nunca imprime o valor. Modelo é exatamente `claude-opus-4-8`.
- **Meta no servidor vai DIRETO ao Graph** (`https://graph.facebook.com/v21.0`), lendo `accounts.access_token` via service role — **não** usa `meta-proxy` (que exige JWT de usuário).
- **Escrita no Meta pelo front** só via `_gtApplyAction`/`metaPost` já existentes, sempre com confirmação `_gtConfirm`. Toasts/modais do iamundi — nunca `alert()` nativo em UI nova.
- **Nunca mexer em campanha/anúncio real durante verificação:** ao testar Aplicar/Pausar, confira os valores no modal de confirmação e **cancele** — não aplique de fato em conta real.
- **Testes:** o robô (`coletor/budget-ia.mjs`) tem lógica pura testada com `node:test`. As mudanças no front (`index.html`, monólito sem harness) e o SQL são verificados **manualmente** contra o preview do branch (Vercel auto-deploya o branch) — cada task de front lista passos exatos e resultado esperado.
- **Gate da ferramenta:** leitura de `gt_budget_analises` na tela usa o helper `sb()` (JWT do usuário); o RLS libera pra `role='admin' OR 'meta.gestor' = any(features)`. A UI nova aparece dentro da Gestão de Tráfego, que já é gateada.
- **RLS:** ninguém além do service role escreve em `gt_budget_analises`.

---

### Task 1: Migration — tabela `gt_budget_analises` + RLS

**Files:**
- Create: `docs/migrations/2026-07-02-gt-budget-analises.sql`

**Interfaces:**
- Produces: tabela `public.gt_budget_analises` (PK `campaign_id`) com as colunas do §6 do spec; leitura liberada pra ferramenta, escrita só service role. Consumida pelo robô (Task 3, upsert) e pela tela (Task 4, select).

- [ ] **Step 1: Criar o arquivo de migration**

Crie `docs/migrations/2026-07-02-gt-budget-analises.sql` com exatamente:

```sql
-- docs/migrations/2026-07-02-gt-budget-analises.sql
-- Sugestões de budget geradas pelo robô semanal (Opus 4.8) por campanha.
-- Leitura liberada pra quem tem a ferramenta Gestão de Tráfego (espelha
-- hasPermission('module:meta:gestor') -> feature 'meta.gestor'; admin sempre).
-- Escrita: NENHUMA policy -> só o service role (que ignora RLS) grava.

create table if not exists public.gt_budget_analises (
  campaign_id            text primary key,
  account_id             text not null,
  objetivo               text,
  effective_status       text,
  budget_atual_centavos  integer,
  budget_sugerido_centavos integer,
  veredito               text,
  justificativa          text,
  impacto_estimado       text,
  modelo                 text not null default 'opus-4-8',
  gerado_em              timestamptz not null default now(),
  valida_ate             timestamptz
);

alter table public.gt_budget_analises enable row level security;

-- leitura só pra quem tem a ferramenta (admin OU feature meta.gestor)
drop policy if exists gt_budget_read on public.gt_budget_analises;
create policy gt_budget_read on public.gt_budget_analises
  for select
  using (
    exists (
      select 1 from public.profiles p
      where p.id = auth.uid()
        and (p.role = 'admin' or 'meta.gestor' = any(p.features))
    )
  );
-- sem policy de insert/update/delete: a tela nunca escreve; só o service role.
```

- [ ] **Step 2: Aplicar a migration**

Rode o runner do projeto (lê `coletor/.env` → `DATABASE_URL`, porta 6543, cert `supabase-ca.crt`):

Run: `node coletor/run-migrations.mjs --dry`
Expected: lista `2026-07-02-gt-budget-analises.sql` como pendente.

Run: `node coletor/run-migrations.mjs`
Expected: `→ 2026-07-02-gt-budget-analises.sql … OK`

> Se `coletor/.env`/`DATABASE_URL` não estiver disponível no ambiente do executor, aplicar o mesmo SQL via Supabase MCP `apply_migration` (name: `gt_budget_analises`).

- [ ] **Step 3: Verificar tabela e RLS**

Rode esta query (via `psql`/Supabase SQL editor/MCP `execute_sql`):

```sql
select
  (select count(*) from information_schema.tables
     where table_schema='public' and table_name='gt_budget_analises') as tabela,
  (select count(*) from pg_policies
     where schemaname='public' and tablename='gt_budget_analises') as policies;
```
Expected: `tabela = 1`, `policies = 1` (só a de leitura).

- [ ] **Step 4: Commit**

```bash
git add docs/migrations/2026-07-02-gt-budget-analises.sql
git commit -m "feat(budget-ia): migration gt_budget_analises + RLS de leitura por ferramenta"
```

---

### Task 2: Robô — lógica pura + testes (sem rede)

**Files:**
- Create: `coletor/budget-ia.mjs`
- Test: `coletor/budget-ia.test.mjs`

**Interfaces:**
- Produces (exportados de `coletor/budget-ia.mjs`):
  - `campanhaEmVeiculacao(camp, agoraMs) -> boolean`
  - `montarMensagens(camp, ins) -> { system: string, user: string }`
  - `parsearSaida(text) -> { budget_sugerido_centavos:int, veredito:string, justificativa:string, impacto_estimado:string } | null`
- Consumed by: Task 3 (o `main()` no mesmo arquivo).

- [ ] **Step 1: Escrever os testes que falham**

Crie `coletor/budget-ia.test.mjs`:

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { campanhaEmVeiculacao, montarMensagens, parsearSaida } from './budget-ia.mjs';

const AGORA = Date.parse('2026-07-02T12:00:00Z');

test('campanhaEmVeiculacao: ACTIVE sem stop_time = veiculando', () => {
  assert.equal(campanhaEmVeiculacao({ effective_status: 'ACTIVE' }, AGORA), true);
});
test('campanhaEmVeiculacao: ACTIVE com stop_time futuro = veiculando', () => {
  assert.equal(campanhaEmVeiculacao({ effective_status: 'ACTIVE', stop_time: '2026-08-01T00:00:00+0000' }, AGORA), true);
});
test('campanhaEmVeiculacao: ACTIVE com stop_time passado = encerrada', () => {
  assert.equal(campanhaEmVeiculacao({ effective_status: 'ACTIVE', stop_time: '2026-06-01T00:00:00+0000' }, AGORA), false);
});
test('campanhaEmVeiculacao: PAUSED = fora', () => {
  assert.equal(campanhaEmVeiculacao({ effective_status: 'PAUSED' }, AGORA), false);
});

test('montarMensagens: inclui objetivo e budget no texto do usuário', () => {
  const { system, user } = montarMensagens(
    { name: 'C1', objective: 'OUTCOME_SALES', daily_budget: '5000' },
    { spend: '120', ctr: '1.5', purchase_roas: [{ value: '3.2' }] }
  );
  assert.match(system, /JSON/);
  assert.match(user, /OUTCOME_SALES/);
  assert.match(user, /5000/);
});

test('parsearSaida: JSON puro válido', () => {
  const o = parsearSaida('{"budget_sugerido_centavos":6000,"veredito":"escalar","justificativa":"ROAS bom","impacto_estimado":"+20% compras"}');
  assert.equal(o.budget_sugerido_centavos, 6000);
  assert.equal(o.veredito, 'escalar');
});
test('parsearSaida: JSON embutido em prosa', () => {
  const o = parsearSaida('Claro! Aqui vai:\n{"budget_sugerido_centavos":3000,"veredito":"reduzir","justificativa":"CPC alto","impacto_estimado":"gasto -25%"}\nEspero ter ajudado.');
  assert.equal(o.veredito, 'reduzir');
});
test('parsearSaida: veredito inválido = null', () => {
  assert.equal(parsearSaida('{"budget_sugerido_centavos":100,"veredito":"turbinar","justificativa":"x","impacto_estimado":"y"}'), null);
});
test('parsearSaida: campo faltando = null', () => {
  assert.equal(parsearSaida('{"veredito":"manter","justificativa":"x","impacto_estimado":"y"}'), null);
});
test('parsearSaida: lixo = null', () => {
  assert.equal(parsearSaida('sem json aqui'), null);
});
```

- [ ] **Step 2: Rodar os testes e ver falhar**

Run: `node --test coletor/budget-ia.test.mjs`
Expected: FALHA — `Cannot find module './budget-ia.mjs'` (arquivo ainda não existe).

- [ ] **Step 3: Criar `coletor/budget-ia.mjs` com as funções puras**

Crie `coletor/budget-ia.mjs` (só a parte pura por enquanto; o `main()` entra na Task 3):

```js
// coletor/budget-ia.mjs
// Robô semanal: analisa campanhas em veiculação com Opus 4.8 e grava sugestões
// de budget em gt_budget_analises. Roda via .github/workflows/budget-ia.yml.

const VEREDITOS = new Set(['escalar', 'reduzir', 'manter', 'pausar']);

// Campanha "em veiculação real": ACTIVE e (sem stop_time OU stop_time no futuro).
export function campanhaEmVeiculacao(camp, agoraMs) {
  if (!camp || camp.effective_status !== 'ACTIVE') return false;
  if (!camp.stop_time) return true;
  const t = Date.parse(camp.stop_time);
  return Number.isNaN(t) ? true : t > agoraMs;
}

// Monta as mensagens (system + user) pro Opus. Pede SOMENTE um JSON.
export function montarMensagens(camp, ins) {
  const system =
    'Você é um gestor de tráfego pago sênior. Analise UMA campanha do Meta Ads e ' +
    'recomende o orçamento diário ideal, respeitando o OBJETIVO da campanha ' +
    '(Vendas: priorize ROAS/CAC; Tráfego: CPC/CTR; Reconhecimento: alcance/CPM; ' +
    'Leads: custo por lead). Seja conservador quando faltar dado. Se o gasto não se ' +
    'justifica pela performance, use veredito "pausar". ' +
    'Responda SOMENTE com um JSON válido, sem texto antes ou depois, no formato: ' +
    '{"budget_sugerido_centavos": <inteiro, orçamento diário em centavos de R$>, ' +
    '"veredito": "escalar"|"reduzir"|"manter"|"pausar", ' +
    '"justificativa": "<1-2 frases em PT-BR>", ' +
    '"impacto_estimado": "<estimativa curta do efeito em PT-BR, ex.: +30% budget → ~+25% compras>"}';
  const dados = {
    nome: camp.name || '',
    objetivo: camp.objective || '',
    budget_diario_atual_centavos: camp.daily_budget != null ? Number(camp.daily_budget) : null,
    budget_total_centavos: camp.lifetime_budget != null ? Number(camp.lifetime_budget) : null,
    gasto: num(ins.spend),
    impressoes: num(ins.impressions),
    cliques: num(ins.clicks),
    ctr_pct: num(ins.ctr),
    cpc: num(ins.cpc),
    alcance: num(ins.reach),
    frequencia: num(ins.frequency),
    roas: Array.isArray(ins.purchase_roas) && ins.purchase_roas[0] ? num(ins.purchase_roas[0].value) : null,
    acoes: ins.actions || null,
    valores_acao: ins.action_values || null,
  };
  const user =
    'Dados da campanha (janela recente):\n' + JSON.stringify(dados) +
    '\nResponda apenas o JSON pedido.';
  return { system, user };
}

// Extrai e valida o JSON da resposta. Retorna o objeto validado ou null.
export function parsearSaida(text) {
  if (!text || typeof text !== 'string') return null;
  const m = text.match(/\{[\s\S]*\}/);
  if (!m) return null;
  let o;
  try { o = JSON.parse(m[0]); } catch { return null; }
  if (!o || typeof o !== 'object') return null;
  const b = o.budget_sugerido_centavos;
  if (!Number.isFinite(b) || b < 0) return null;
  if (!VEREDITOS.has(o.veredito)) return null;
  if (typeof o.justificativa !== 'string' || !o.justificativa.trim()) return null;
  if (typeof o.impacto_estimado !== 'string' || !o.impacto_estimado.trim()) return null;
  return {
    budget_sugerido_centavos: Math.round(b),
    veredito: o.veredito,
    justificativa: o.justificativa.trim(),
    impacto_estimado: o.impacto_estimado.trim(),
  };
}

function num(v) { const n = parseFloat(v); return Number.isFinite(n) ? n : null; }
```

- [ ] **Step 4: Rodar os testes e ver passar**

Run: `node --test coletor/budget-ia.test.mjs`
Expected: PASS — 9 testes passando, 0 falhas.

- [ ] **Step 5: Commit**

```bash
git add coletor/budget-ia.mjs coletor/budget-ia.test.mjs
git commit -m "feat(budget-ia): lógica pura do robô (veiculação, prompt, parse) + testes"
```

---

### Task 3: Robô — wiring (Graph + Opus + upsert) + workflow

**Files:**
- Modify: `coletor/budget-ia.mjs` (adicionar helpers de rede e `main()`)
- Create: `.github/workflows/budget-ia.yml`

**Interfaces:**
- Consumes: `campanhaEmVeiculacao`, `montarMensagens`, `parsearSaida` (Task 2); tabela `gt_budget_analises` (Task 1).
- Produces: linhas em `gt_budget_analises` (uma por campanha em veiculação), gravadas por `main()`.

- [ ] **Step 1: Adicionar env, helpers Supabase/Graph/Anthropic e `main()`**

Acrescente ao FINAL de `coletor/budget-ia.mjs` (mantendo as funções puras acima):

```js
// ---------- infra (rede) — só roda no main(), não é importado nos testes ----------
const ANTHROPIC_API_KEY = process.env.ANTHROPIC_API_KEY_BUDGET || process.env.ANTHROPIC_API_KEY;
const SERVICE_KEY = process.env.SUPABASE_SERVICE_KEY;
const SUPABASE_URL = process.env.SUPABASE_URL || 'https://kounqtdoioootxqegkij.supabase.co';
const MODEL = process.env.BUDGET_MODEL || 'claude-opus-4-8';
const GRAPH = 'https://graph.facebook.com/v21.0';
const REST = SUPABASE_URL + '/rest/v1';
const DRY = process.argv.includes('--dry');

const sbHeaders = { apikey: SERVICE_KEY, Authorization: 'Bearer ' + SERVICE_KEY, 'Content-Type': 'application/json' };
const sleep = (ms) => new Promise((r) => setTimeout(r, ms));

async function sbGet(path) {
  const r = await fetch(REST + path, { headers: sbHeaders });
  if (!r.ok) throw new Error('REST GET ' + path + ' -> ' + r.status + ' ' + (await r.text()).slice(0, 200));
  return r.json();
}
async function sbUpsert(path, body) {
  const r = await fetch(REST + path, {
    method: 'POST',
    headers: { ...sbHeaders, Prefer: 'resolution=merge-duplicates,return=minimal' },
    body: JSON.stringify(body),
  });
  if (!r.ok && ![200, 201, 204].includes(r.status)) {
    throw new Error('REST POST ' + path + ' -> ' + r.status + ' ' + (await r.text()).slice(0, 200));
  }
  return r;
}
function cleanAcc(id) { return String(id || '').replace(/^act_/, ''); }
async function graphGet(path, params, token) {
  const url = new URL(GRAPH + path);
  Object.entries(params).forEach(([k, v]) => url.searchParams.set(k, typeof v === 'object' ? JSON.stringify(v) : String(v)));
  url.searchParams.set('access_token', token);
  const r = await fetch(url.toString());
  if (!r.ok) throw new Error('Graph ' + path + ' -> ' + r.status + ' ' + (await r.text()).slice(0, 200));
  return r.json();
}

async function anthropic(body, tentativas = 6) {
  for (let t = 0; t < tentativas; t++) {
    let r;
    try {
      r = await fetch('https://api.anthropic.com/v1/messages', {
        method: 'POST',
        headers: { 'x-api-key': ANTHROPIC_API_KEY, 'anthropic-version': '2023-06-01', 'content-type': 'application/json' },
        body: JSON.stringify(body),
      });
    } catch { await sleep(Math.min(60, 8 * (t + 1)) * 1000); continue; }
    if (r.ok) return r.json();
    if (r.status === 429 || r.status >= 500) {
      const ra = parseInt(r.headers.get('retry-after') || '0', 10);
      await sleep((ra > 0 ? ra : Math.min(60, 8 * (t + 1))) * 1000);
      continue;
    }
    throw new Error('Anthropic ' + r.status + ' ' + (await r.text()).slice(0, 300));
  }
  throw new Error('Anthropic: tentativas esgotadas');
}

// Extrai o texto (fora dos blocos de thinking) e detecta refusal.
function textoDaResposta(resp) {
  if (resp && resp.stop_reason === 'refusal') return null;
  const blocks = (resp && resp.content) || [];
  return blocks.filter((b) => b && b.type === 'text').map((b) => b.text).join('').trim();
}

async function main() {
  if (!ANTHROPIC_API_KEY || !SERVICE_KEY) {
    console.error('✗ Faltam segredos: ANTHROPIC_API_KEY_BUDGET, SUPABASE_SERVICE_KEY');
    process.exit(1);
  }
  const agoraMs = Date.now();
  const proximaSegunda = new Date(agoraMs + 7 * 86400000).toISOString();
  const iso = (d) => d.toISOString().slice(0, 10);
  const since = iso(new Date(agoraMs - 7 * 86400000));
  const until = iso(new Date(agoraMs));

  // Sem assumir coluna de "tool": pega todas e filtra as que têm ad_account_id + token
  // (contas de anúncio do Meta têm ad_account_id; outras ficam de fora).
  const contas = await sbGet('/accounts?select=id,ad_account_id,access_token');
  let total = 0, gravadas = 0, puladas = 0;

  for (const acc of contas) {
    if (!acc.ad_account_id || !acc.access_token) continue;
    const adAcc = cleanAcc(acc.ad_account_id);
    const campFields = 'id,name,effective_status,objective,daily_budget,lifetime_budget,start_time,stop_time';
    const insFields = 'campaign_id,impressions,clicks,spend,ctr,cpc,reach,frequency,actions,action_values,purchase_roas,objective';
    let camps, insights;
    try {
      camps = (await graphGet(`/act_${adAcc}/campaigns`, { fields: campFields, effective_status: ['ACTIVE'], limit: 500 }, acc.access_token)).data || [];
      insights = (await graphGet(`/act_${adAcc}/insights`, { level: 'campaign', fields: insFields, time_range: { since, until }, limit: 500 }, acc.access_token)).data || [];
    } catch (e) { console.log('  conta ' + acc.id + ' falhou no Graph: ' + e.message); continue; }

    const insByCamp = {};
    insights.forEach((i) => { insByCamp[i.campaign_id] = i; });
    const ativas = camps.filter((c) => campanhaEmVeiculacao(c, agoraMs));
    console.log(`Conta ${acc.id}: ${ativas.length} campanhas em veiculação.`);

    for (const camp of ativas) {
      total++;
      const ins = insByCamp[camp.id] || {};
      const { system, user } = montarMensagens(camp, ins);
      if (DRY) { console.log('  [dry] ' + (camp.name || camp.id)); continue; }
      let saida;
      try {
        const resp = await anthropic({ model: MODEL, max_tokens: 1024, thinking: { type: 'adaptive' }, system, messages: [{ role: 'user', content: user }] });
        saida = parsearSaida(textoDaResposta(resp));
      } catch (e) { console.log('  ✗ ' + (camp.name || camp.id) + ': ' + e.message); puladas++; continue; }
      if (!saida) { console.log('  ⚠ ' + (camp.name || camp.id) + ': sem sugestão válida'); puladas++; continue; }
      await sbUpsert('/gt_budget_analises', [{
        campaign_id: camp.id,
        account_id: acc.id,
        objetivo: camp.objective || null,
        effective_status: camp.effective_status || null,
        budget_atual_centavos: camp.daily_budget != null ? Number(camp.daily_budget) : null,
        budget_sugerido_centavos: saida.budget_sugerido_centavos,
        veredito: saida.veredito,
        justificativa: saida.justificativa,
        impacto_estimado: saida.impacto_estimado,
        modelo: MODEL,
        gerado_em: new Date().toISOString(),
        valida_ate: proximaSegunda,
      }]);
      gravadas++;
    }
  }
  console.log(`Concluído: ${total} analisadas, ${gravadas} gravadas, ${puladas} puladas.`);
}

// Só roda main() quando executado como script (não quando importado nos testes).
if (import.meta.url === `file://${process.argv[1]}`) {
  main().catch((e) => { console.error(e); process.exit(1); });
}
```

- [ ] **Step 2: Confirmar que os testes puros continuam passando (import não dispara main)**

Run: `node --test coletor/budget-ia.test.mjs`
Expected: PASS — 9 testes. (O guard `import.meta.url === ...` impede o `main()` de rodar no import.)

- [ ] **Step 3: Criar o workflow do GitHub Actions**

Crie `.github/workflows/budget-ia.yml`:

```yaml
name: Budget IA (análise semanal)

# Segunda 09:30 UTC (06:30 Brasília) + disparo manual.
on:
  schedule:
    - cron: '30 9 * * 1'
  workflow_dispatch: {}

concurrency:
  group: budget-ia
  cancel-in-progress: false

jobs:
  analise:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Rodar análise de budget
        env:
          ANTHROPIC_API_KEY_BUDGET: ${{ secrets.ANTHROPIC_API_KEY_BUDGET }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: node coletor/budget-ia.mjs
```

- [ ] **Step 4: Verificação de integração (após o Breno cadastrar os secrets)**

> Requer os secrets `ANTHROPIC_API_KEY_BUDGET` (chave nova/dedicada, revogando a antiga exposta) e `SUPABASE_SERVICE_KEY` já existente no repo. A cobertura automática do núcleo é a Task 2; este passo é a checagem end-to-end.

No GitHub: aba **Actions → Budget IA (análise semanal) → Run workflow** (branch `feat/gt-budget-ia`).
Expected: run verde; log mostra "Conta … N campanhas em veiculação" e "Concluído: X analisadas, Y gravadas".

Depois, confira uma linha gravada (SQL editor/MCP `execute_sql`):
```sql
select campaign_id, veredito, budget_sugerido_centavos, left(justificativa,40) as just, gerado_em
from public.gt_budget_analises order by gerado_em desc limit 5;
```
Expected: linhas recentes com `veredito` ∈ (escalar/reduzir/manter/pausar) e `budget_sugerido_centavos` > 0.

- [ ] **Step 5: Commit**

```bash
git add coletor/budget-ia.mjs .github/workflows/budget-ia.yml
git commit -m "feat(budget-ia): robô Opus semanal (Graph direto + upsert) + workflow"
```

---

### Task 4: Front — ler `gt_budget_analises` e mostrar a sugestão do Opus

**Files:**
- Modify: `index.html` (`loadGtData` ~L7950; `_renderGtCampaigns` ~L8164; adicionar helper e global perto de `_gtLoadConfig` ~L7876)

**Interfaces:**
- Consumes: tabela `gt_budget_analises` (Task 1); helper `sb()` (L3296); `_gtEsc`, `_maFmtR` já existentes.
- Produces: global `_gtBudgetIA` (mapa `campaign_id -> row`); função `_gtIABlocoHtml(row)` que devolve HTML da sugestão; usados na Task 5.

- [ ] **Step 1: Adicionar global e carregador da tabela**

Perto de `_gtLoadConfig` (L7876) adicione a global e a função:

```javascript
let _gtBudgetIA={};
async function _gtLoadBudgetIA(){
  try{
    const rows=await sb('gt_budget_analises?select=campaign_id,budget_sugerido_centavos,veredito,justificativa,impacto_estimado,gerado_em,budget_atual_centavos');
    _gtBudgetIA={};
    (rows||[]).forEach(r=>{ if(r&&r.campaign_id) _gtBudgetIA[r.campaign_id]=r; });
  }catch(e){ _gtBudgetIA={}; }
}
```

- [ ] **Step 2: Chamar o carregador em `loadGtData`**

Em `loadGtData`, logo após a linha que carrega a config (L7975 `if(!_gtConfigLoaded){ await _gtLoadConfig(); _gtConfigLoaded=true; }`), adicione:

```javascript
    await _gtLoadBudgetIA();
```

- [ ] **Step 3: Adicionar o helper de HTML da sugestão**

Adicione junto das outras funções `_gt*` (ex.: após `_gtIABlocoHtml` não existe ainda — coloque logo antes de `_renderGtCampaigns`, ~L8163):

```javascript
// Bloco visual da sugestão do Opus (ou null se não houver). row = _gtBudgetIA[campId].
function _gtIABlocoHtml(row){
  if(!row)return '';
  const sug=row.budget_sugerido_centavos!=null?_maFmtR(row.budget_sugerido_centavos/100):'—';
  const quando=row.gerado_em?new Date(row.gerado_em).toLocaleDateString('pt-BR',{weekday:'short',day:'2-digit',month:'2-digit'}):'';
  const verMap={escalar:'#16a34a',reduzir:'#d97706',manter:'#6366f1',pausar:'#dc2626'};
  const cor=verMap[row.veredito]||'#6366f1';
  return `<div class="gt-ia-sug" style="margin-top:7px;padding:8px 10px;border:1px solid var(--border);border-left:3px solid ${cor};border-radius:8px;background:var(--surface2);">
    <div style="display:flex;align-items:center;gap:8px;flex-wrap:wrap;">
      <span style="font-size:9px;font-weight:800;letter-spacing:.5px;text-transform:uppercase;color:${cor};">✦ IA · ${_gtEsc(quando)}</span>
      <span style="font-family:'IBM Plex Sans',sans-serif;font-size:11px;font-weight:700;color:var(--text);">Budget sugerido: ${sug}/dia</span>
      <span style="font-size:10px;font-weight:700;color:${cor};text-transform:uppercase;">${_gtEsc(row.veredito||'')}</span>
    </div>
    <div style="font-family:'IBM Plex Sans',sans-serif;font-size:11px;color:var(--muted);margin-top:4px;line-height:1.45;">${_gtEsc(row.justificativa||'')}</div>
    ${row.impacto_estimado?`<div style="font-family:'IBM Plex Sans',sans-serif;font-size:10px;color:var(--muted);margin-top:3px;"><b>Impacto estimado (IA):</b> ${_gtEsc(row.impacto_estimado)}</div>`:''}
  </div>`;
}
```

- [ ] **Step 4: Renderizar o bloco por campanha**

Em `_renderGtCampaigns`, dentro de `renderList`, logo após a linha que cria o `actBar` (L8269 `const actBar=_gtRenderActions(sug.actions,row);`), adicione a montagem do bloco IA e inclua no `inner` (antes do `if(actBar)inner.appendChild(actBar);` em L8271):

```javascript
      const iaRow=_gtBudgetIA[ins.campaign_id];
      const iaBloco=document.createElement('div');
      iaBloco.innerHTML=_gtIABlocoHtml(iaRow);
      if(iaRow)inner.appendChild(iaBloco.firstElementChild);
```

- [ ] **Step 5: Verificação manual (preview do branch)**

Suba o branch (`git push`) e abra o preview do Vercel do branch `feat/gt-budget-ia`. Faça login, vá em **Gestão de Tráfego**, selecione uma conta com dados.
Expected: campanhas que têm linha em `gt_budget_analises` mostram o bloco "✦ IA · <dia> · Budget sugerido: R$ X/dia · <veredito>" com justificativa e impacto. Campanhas sem linha continuam iguais (sem o bloco), sem erro no console.

> Se a tabela ainda estiver vazia (robô não rodou), insira 1 linha de teste via SQL só pra validar o render, depois apague:
> ```sql
> insert into public.gt_budget_analises(campaign_id,account_id,budget_sugerido_centavos,veredito,justificativa,impacto_estimado)
> values ('<UM_campaign_id_real_da_conta>','<account_id>',6000,'escalar','ROAS acima da média; espaço pra escalar.','+30% budget → ~+25% compras');
> ```

- [ ] **Step 6: Commit**

```bash
git add index.html
git commit -m "feat(budget-ia): tela lê gt_budget_analises e mostra sugestão do Opus por campanha"
```

---

### Task 5: Front — botão [Aplicar sugestão] + campo de budget manual

**Files:**
- Modify: `index.html` (`_gtIABlocoHtml` da Task 4 e o render em `_renderGtCampaigns`)

**Interfaces:**
- Consumes: `_gtBudgetIA` e `_gtIABlocoHtml` (Task 4); `_gtApplyAction(action,btn,rowEl)` (L8384), `_maFmtR`, `_gtEsc` já existentes.
- Produces: função `_gtWireBudgetControls(container, ins, camp, iaRow)` que liga os botões dentro do bloco renderizado.

- [ ] **Step 1: Incluir os controles no HTML do bloco**

Em `_gtIABlocoHtml` (Task 4), antes do `</div>` final do bloco, acrescente a linha de controles (usa `data-*` pra ligar depois; sempre presentes, mesmo sem `daily_budget` — o backend valida):

```javascript
    <div class="gt-ia-ctrls" style="display:flex;align-items:center;gap:6px;flex-wrap:wrap;margin-top:8px;">
      <button data-gt-aplicar="1" class="gt-act-btn primary" style="font-size:11px;">Aplicar ${sug}/dia</button>
      <span style="font-size:10px;color:var(--muted);">ou</span>
      <input data-gt-manual="1" type="number" min="1" step="1" placeholder="R$/dia" style="width:80px;padding:5px 7px;border:1px solid var(--border);border-radius:6px;background:var(--surface);color:var(--text);font-family:'IBM Plex Sans',sans-serif;font-size:11px;">
      <button data-gt-manual-ok="1" class="gt-act-btn" style="font-size:11px;">Aplicar manual</button>
    </div>
```

> Se `row.budget_sugerido_centavos` for null, o texto do botão fica "Aplicar —/dia" — trate desabilitando-o na ligação (Step 2).

- [ ] **Step 2: Ligar os botões após inserir o bloco**

Adicione a função (perto de `_gtIABlocoHtml`):

```javascript
function _gtWireBudgetControls(el,ins,camp,iaRow){
  if(!el)return;
  const nm=_gtEsc(ins.campaign_name||camp?.name||'a campanha');
  const daily=camp?.daily_budget?parseFloat(camp.daily_budget)/100:null;
  const bAplicar=el.querySelector('[data-gt-aplicar]');
  if(bAplicar){
    if(!iaRow||iaRow.budget_sugerido_centavos==null){bAplicar.disabled=true;bAplicar.style.opacity=.5;}
    else bAplicar.addEventListener('click',ev=>{ev.stopPropagation();
      const novo=iaRow.budget_sugerido_centavos;
      _gtApplyAction({type:'update_budget',id:ins.campaign_id,budget:novo,_t:'Aplicar budget sugerido?',_d:`"${nm}": ${daily!=null?_maFmtR(daily)+'/dia':'orçamento atual'} → ${_maFmtR(novo/100)}/dia (sugestão da IA).`},bAplicar,el);
    });
  }
  const inp=el.querySelector('[data-gt-manual]'),bMan=el.querySelector('[data-gt-manual-ok]');
  if(bMan&&inp)bMan.addEventListener('click',ev=>{ev.stopPropagation();
    const v=parseFloat(inp.value);
    if(!Number.isFinite(v)||v<=0){inp.style.borderColor='#dc2626';return;}
    inp.style.borderColor='';
    const cent=Math.round(v*100);
    _gtApplyAction({type:'update_budget',id:ins.campaign_id,budget:cent,_t:'Aplicar budget manual?',_d:`"${nm}": ${daily!=null?_maFmtR(daily)+'/dia':'orçamento atual'} → ${_maFmtR(v)}/dia.`},bMan,el);
  });
}
```

- [ ] **Step 3: Chamar a ligação no render**

Em `_renderGtCampaigns`, onde o bloco IA é anexado (Task 4 Step 4), troque o append por uma versão que também liga os controles:

```javascript
      const iaRow=_gtBudgetIA[ins.campaign_id];
      if(iaRow){
        const iaBloco=document.createElement('div');
        iaBloco.innerHTML=_gtIABlocoHtml(iaRow);
        const el=iaBloco.firstElementChild;
        inner.appendChild(el);
        _gtWireBudgetControls(el,ins,camp,iaRow);
      }
```

(remova o append anterior da Task 4 Step 4, substituído por este.)

- [ ] **Step 4: Verificação manual (preview)**

Push, abra o preview, vá numa campanha com sugestão IA.
Expected:
- Clicar **Aplicar R$X/dia** abre o modal `_gtConfirm` mostrando "de <atual>/dia → <X>/dia (sugestão da IA)". **Cancele** (não aplique em conta real).
- Digitar `50` e clicar **Aplicar manual** abre o modal "…→ R$ 50,00/dia". **Cancele.**
- Campo vazio/inválido: a borda do input fica vermelha e nada abre.

- [ ] **Step 5: Commit**

```bash
git add index.html
git commit -m "feat(budget-ia): botão Aplicar sugestão + campo de budget manual"
```

---

### Task 6: Front — pausar/reativar SEMPRE (campanha e anúncio)

**Files:**
- Modify: `index.html` (`_renderGtCampaigns` ~L8269; `_renderGtAds` ~L8315)

**Interfaces:**
- Consumes: `_gtApplyAction`, `_gtEsc`, ações `pause_campaign`/`activate_campaign`/`pause_ad`/`activate_ad` (já suportadas em `_gtApplyAction` L8394-8398).
- Produces: função `_gtManualToggleBtn(kind, id, status, nome)` que devolve um `<button>` (ou null) com o toggle manual conforme o status.

- [ ] **Step 1: Helper do botão de toggle manual**

Adicione perto de `_gtRenderActions` (L8154):

```javascript
// Botão manual de pausar/reativar SEMPRE disponível, independente do veredito.
// kind='campaign'|'ad'. Retorna o <button> ou null (para status sem toggle).
function _gtManualToggleBtn(kind,id,status,nome){
  const nm=_gtEsc(nome||(kind==='ad'?'o anúncio':'a campanha'));
  let action=null;
  if(status==='ACTIVE'){
    action=kind==='ad'
      ?{type:'pause_ad',id,_t:'Pausar anúncio?',_d:`"${nm}" será PAUSADO na Meta agora.`}
      :{type:'pause_campaign',id,_t:'Pausar campanha?',_d:`"${nm}" será PAUSADA na Meta agora.`};
  }else if(status==='PAUSED'){
    action=kind==='ad'
      ?{type:'activate_ad',id,_t:'Ativar anúncio?',_d:`"${nm}" voltará a ATIVO e a gastar na Meta.`}
      :{type:'activate_campaign',id,_t:'Reativar campanha?',_d:`"${nm}" voltará a ATIVA e a gastar na Meta.`};
  }
  if(!action)return null; // ARCHIVED/encerrada: sem toggle
  const b=document.createElement('button');
  b.className='gt-act-btn '+(status==='ACTIVE'?'':'success');
  b.textContent=status==='ACTIVE'?'⏸ Pausar':'▶ Reativar';
  b.style.opacity='.9';
  b.addEventListener('click',e=>{e.stopPropagation();_gtApplyAction(action,b,null);});
  return b;
}
```

- [ ] **Step 2: Sempre oferecer o toggle na campanha**

Em `_renderGtCampaigns`, após o `actBar` (L8269), garanta uma barra de ação mesmo sem sugestões do motor, e anexe o toggle manual:

```javascript
      let actBar2=actBar;
      const campStatus=camp?.effective_status||'';
      const tgl=_gtManualToggleBtn('campaign',ins.campaign_id,campStatus,ins.campaign_name||camp?.name);
      if(tgl){ if(!actBar2){actBar2=document.createElement('div');actBar2.className='gt-action-row';} actBar2.appendChild(tgl); }
```

e troque o `if(actBar)inner.appendChild(actBar);` (L8271) por `if(actBar2)inner.appendChild(actBar2);`.

- [ ] **Step 3: Sempre oferecer o toggle no anúncio**

Em `_renderGtAds`, onde monta `actBar` (L8315), aplique o mesmo:

```javascript
    let actBar=_gtRenderActions(sug.actions,card);
    const adTgl=_gtManualToggleBtn('ad',ad.ad_id,adStatus,ad.ad_name||ad.adset_name);
    if(adTgl){ if(!actBar){actBar=document.createElement('div');actBar.className='gt-action-row';} actBar.appendChild(adTgl); }
```

(A linha original `const actBar=_gtRenderActions(sug.actions,card);` vira `let actBar=...`.)

- [ ] **Step 4: Verificação manual (preview)**

Push, abra o preview.
Expected:
- Uma campanha **ACTIVE saudável** (que antes não tinha botão) agora mostra **⏸ Pausar**. Clicar abre o modal "será PAUSADA…". **Cancele.**
- Uma campanha **PAUSED** mostra **▶ Reativar**.
- Expanda os anúncios: um anúncio **ACTIVE saudável** agora mostra **⏸ Pausar**; um **PAUSED** mostra **▶ Reativar**. **Cancele** os modais.
- Campanha/anúncio **ARCHIVED**: sem toggle.

- [ ] **Step 5: Commit**

```bash
git add index.html
git commit -m "feat(budget-ia): pausar/reativar sempre disponível em campanha e anúncio"
```

---

### Task 7: Front — status "Encerrada" (stop_time passado)

**Files:**
- Modify: `index.html` (`_renderGtCampaigns` ~L8221-8248)

**Interfaces:**
- Consumes: `camp.stop_time`, `camp.effective_status` (já em `campFields`); `_gtManualToggleBtn` (Task 6).
- Produces: helper `_gtEncerrada(camp, nowMs)`; badge "Encerrada" e supressão de toggle/sugestão para campanhas vencidas.

- [ ] **Step 1: Helper de "encerrada"**

Adicione perto de `_gtManualToggleBtn`:

```javascript
// Campanha "encerrada": ACTIVE no Meta mas com stop_time já no passado.
function _gtEncerrada(camp,nowMs){
  if(!camp||camp.effective_status!=='ACTIVE'||!camp.stop_time)return false;
  const t=Date.parse(camp.stop_time);
  return !Number.isNaN(t)&&t<nowMs;
}
```

- [ ] **Step 2: Aplicar no render da campanha**

Em `_renderGtCampaigns`, dentro de `renderList`, logo após `const status=camp?.effective_status||'';` (L8223), calcule o encerramento e ajuste o badge:

```javascript
      const encerrada=_gtEncerrada(camp,Date.now());
```

Troque a montagem do badge (L8244-8247) para tratar "encerrada" (mesmo bloco que hoje decide `badgeCls`/`badgeLbl`):

```javascript
      const badge=document.createElement('div');
      const badgeCls=encerrada?'inactive':(status==='ACTIVE'?'active':status==='PAUSED'?'paused':'inactive');
      const badgeLbl=encerrada?'Encerrada':(status==='ACTIVE'?'Ativo':status==='PAUSED'?'Pausado':status==='ARCHIVED'?'Arquivado':'Inativo');
      badge.className=`gt-status-badge ${badgeCls}`;badge.textContent=badgeLbl;
```

- [ ] **Step 3: Suprimir toggle e sugestão em campanha encerrada**

No trecho da Task 6 Step 2 (toggle manual), condicione ao `!encerrada`; e no bloco IA (Task 5 Step 3) idem. Ou seja, os dois passam a começar com `if(!encerrada){ … }`. Exemplo para o toggle:

```javascript
      let actBar2=actBar;
      if(!encerrada){
        const campStatus=camp?.effective_status||'';
        const tgl=_gtManualToggleBtn('campaign',ins.campaign_id,campStatus,ins.campaign_name||camp?.name);
        if(tgl){ if(!actBar2){actBar2=document.createElement('div');actBar2.className='gt-action-row';} actBar2.appendChild(tgl); }
      }
```

e o bloco IA (Task 5 Step 3):

```javascript
      const iaRow=_gtBudgetIA[ins.campaign_id];
      if(iaRow&&!encerrada){
        const iaBloco=document.createElement('div');
        iaBloco.innerHTML=_gtIABlocoHtml(iaRow);
        const el=iaBloco.firstElementChild;
        inner.appendChild(el);
        _gtWireBudgetControls(el,ins,camp,iaRow);
      }
```

- [ ] **Step 4: Verificação manual (preview)**

Push, abra o preview. Encontre (ou crie no Meta, fora do escopo) uma campanha ACTIVE com `stop_time` no passado — ou valide a lógica temporariamente inspecionando uma cujo prazo já venceu.
Expected: ela aparece com selo **"Encerrada"** (cinza), **sem** botão de pausar e **sem** bloco de sugestão IA. Campanhas ACTIVE sem prazo ou com prazo futuro continuam normais.

- [ ] **Step 5: Commit**

```bash
git add index.html
git commit -m "feat(budget-ia): campanha vencida (stop_time passado) vira 'Encerrada'"
```

---

## Notas de execução

- **Ordem:** T1 → T2 → T3 (robô/servidor), depois T4 → T5 → T6 → T7 (front). T4-T7 mexem nas MESMAS funções de `index.html` — executar em sequência, não em paralelo.
- **Secrets (o Breno faz, o Claude não vê):** `ANTHROPIC_API_KEY_BUDGET` (chave nova; **revogar a que foi colada no chat**). `SUPABASE_SERVICE_KEY` já existe.
- **Deploy:** o merge de `feat/gt-budget-ia` em `main` faz o Vercel publicar. O robô só roda quando o workflow existir em `main` (ou por `workflow_dispatch` no branch).
- **Não conserta o drift do meta-proxy** (registrado no spec §0) — fora de escopo.
